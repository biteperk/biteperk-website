---
/**
 * Site footer. 4-column nav driven by `footerColumns` in src/data/nav.ts,
 * a brand blurb on the left, and a bottom bar with copyright, ABN and
 * the "Made in Sydney." signature.
 */
import StarMark from "@/components/StarMark.astro";
import { site } from "@/data/site";
import { footerColumns } from "@/data/nav";

const year = new Date().getFullYear();
const hasAbn = site.abn !== "TODO";
---

<footer>
  <div class="wrap">
    <div class="foot-inner">
      <div class="foot-brand-cell">
        <div class="brand">
          <StarMark size={32} />
          <span class="brand-name foot-brand-name">
            bite<span class="perk">perk</span>
          </span>
        </div>
        <p class="foot-brand-text">
          Biteperk builds voice and AI tools for hospitality. Our first product, VocoTable,
          answers every restaurant call in a warm Australian voice. Made in Sydney.
        </p>
      </div>
      <div class="foot-cols">
        {footerColumns.map((col) => (
          <div class="foot-col">
            <h4>{col.heading}</h4>
            {col.links.map((link) => (
              <a
                href={link.href}
                rel={link.external ? "noopener" : undefined}
                target={link.external ? "_blank" : undefined}
              >
                {link.label}
              </a>
            ))}
          </div>
        ))}
      </div>
    </div>
    <div class="foot-bottom">
      <div>
        © {year} Biteperk Pty Ltd
        {hasAbn && <span> · ABN {site.abn}</span>}
        · All rights reserved.
      </div>
      <div class="foot-tag">
        <span class="dot" aria-hidden="true"></span>
        Made in {site.address.city}.
      </div>
    </div>
  </div>
</footer>

<style>
  footer {
    border-top: 1px solid var(--line);
    padding: 64px 0 36px;
    padding-bottom: calc(36px + env(safe-area-inset-bottom));
    background: var(--ink-0);
  }
  .foot-inner {
    display: grid;
    grid-template-columns: 1.1fr 2.4fr;
    gap: 56px;
  }
  .foot-brand-name { font-size: 17px; }
  .foot-brand-text {
    color: var(--mist);
    font-size: 14px;
    line-height: 1.65;
    margin-top: 16px;
    max-width: 340px;
  }
  .foot-cols {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 28px;
  }
  .foot-col h4 {
    font-size: 11.5px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--white);
    margin-bottom: 18px;
    font-weight: 600;
  }
  .foot-col a {
    display: block;
    font-size: 14px;
    color: var(--mist);
    margin-bottom: 10px;
    transition: color var(--dur-fast) var(--ease-out);
  }
  .foot-col a:hover,
  .foot-col a:focus-visible {
    color: var(--white);
  }
  .foot-bottom {
    margin-top: 56px;
    padding-top: 24px;
    border-top: 1px solid var(--line);
    font-size: 12.5px;
    color: var(--mist);
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 12px;
  }
  .foot-tag {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    color: var(--mist-2);
  }
  .foot-tag .dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    background: var(--gold);
    box-shadow: 0 0 10px var(--gold);
  }

  @media (max-width: 980px) {
    .foot-inner { grid-template-columns: 1fr; gap: 40px; }
    .foot-cols { grid-template-columns: 1fr 1fr; gap: 32px 24px; }
  }
  @media (max-width: 480px) {
    .foot-cols { grid-template-columns: 1fr; }
  }
</style>
