{
  "name": "biteperk-website",
  "type": "module",
  "version": "0.2.0",
  "private": true,
  "description": "Biteperk company website — biteperk.com.au",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "build:global": "BUILD_TARGET=global astro build && node scripts/prune-global.mjs && node scripts/llms-global.mjs",
    "build:site": "npm run build && npm run build:global && node scripts/merge-dist.mjs",
    "gates:au": "node scripts/check-routes.mjs && node scripts/check-schema.mjs && node scripts/check-cities.mjs && node scripts/check-assets.mjs && node scripts/check-links.mjs && node scripts/check-contrast.mjs",
    "gates:global": "BUILD_TARGET=global node scripts/check-routes.mjs && BUILD_TARGET=global node scripts/check-schema.mjs && BUILD_TARGET=global node scripts/check-assets.mjs && BUILD_TARGET=global node scripts/check-links.mjs && node scripts/check-hreflang.mjs",
    "preview": "astro preview",
    "astro": "astro",
    "check": "astro check",
    "test:e2e": "playwright test",
    "lhci": "lhci autorun",
    "og": "node scripts/generate-og.mjs",
    "images:fetch": "node scripts/fetch-images.mjs",
    "images:grade": "node scripts/grade.mjs",
    "images:composite": "node scripts/generate-images.mjs",
    "images": "npm run images:fetch && npm run images:grade && npm run images:composite",
    "images:v2": "node scripts/img/process.mjs",
    "check:images": "node scripts/check-images.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.16.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@axe-core/playwright": "^4.12.1",
    "@fontsource-variable/inter": "^5.2.8",
    "@fontsource/source-serif-4": "^5.2.9",
    "@lhci/cli": "^0.15.1",
    "@playwright/test": "^1.61.1",
    "@resvg/resvg-js": "^2.6.2",
    "satori": "^0.26.0",
    "satori-html": "^0.3.2",
    "sharp": "^0.34.5",
    "typescript": "^5.9.3"
  }
}
